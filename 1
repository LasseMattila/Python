d = {"cat": "kissa"}
while True:
    word = input("Search for a word: ")
    if not word: break
    if word in d: 
        print(word, "=", d[word])
    else:
        definition = input("Word not found. Please input a definition for word %s: " % word)
        if definition:
            d[word] = definition
